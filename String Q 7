#include <bits/stdc++.h>
using namespace std;


int main() {
	int t;
	cin >> t;
	while(t--){
	    string s;
	    string p;
	    cin>>s;
	    cin>>p;
	    auto isFound=s.find(p);
	    if(isFound!=string::npos)
	        cout<<"1"<<endl;
	    else
	        cout<<"0"<<endl;
	}
	
	return 0;
}
