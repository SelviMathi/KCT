#include<iostream>
#include<vector>
using namespace std;

int main() {
	vector<int> vec;
	int n, num, pos;
	cin >> n;
	for(int i = 0; i < n; i ++){
	    cin >> num;
	    vec.push_back(num);
	}
	cin >> pos;
	cout << vec.at(pos);
	
	return 0;
}
